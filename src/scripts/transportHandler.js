const transportOpen = document.getElementById('btn-transport');
const transportClose = document.getElementById('btn-transport-close');
const transportConfirm = document.getElementById('btn-transport-confirm');

const dialogTransport = document.getElementById('dialog-transport');
const formTransport = document.getElementById('form-transport');
const formAirport = document.getElementById('form-airport');
